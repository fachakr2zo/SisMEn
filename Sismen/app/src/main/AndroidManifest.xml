<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.silver23.sismen">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistrarActivity" />
        <activity android:name=".AyudaActivity" />
        <activity
            android:name=".PrincipalActivity"
            android:label="@string/title_activity_principal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".GestionesActivity" />
        <activity android:name=".PersonaActivity" />
        <activity android:name=".HortalizasActivity" />
        <activity android:name=".MenusActivity" />
        <activity android:name=".IngredientesActivity" />
        <activity android:name=".EnfermedadActivity" />
        <activity android:name=".NutricionistaActivity" />
        <activity android:name=".CuentasActivity"></activity>
    </application>

</manifest>